Telegram index bot
for find group,channel,bot

```bash
useradd elasticsearch # 添加用户
passwd elasticsearch # 加上密码
```

vim setup.sh 
```bash
...
user="elasticsearch"
...
```


```bash
chmod +x ./setup.sh
./setup.sh
sudo ./setup.sh install
```
